"use strict";

var _react = _interopRequireDefault(require("react"));

var _enzyme = require("enzyme");

var _Divider = _interopRequireDefault(require("../Divider"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

describe('Divider Specs', function () {
  test('should capitalize its content', function () {
    var content = 'uncapped';
    var divider = (0, _enzyme.mount)(_react["default"].createElement(_Divider["default"], null, content));
    var expected = content.charAt(0).toUpperCase();
    var actual = divider.text().charAt(0);
    expect(actual).toBe(expected);
  });
  test('should render a Divider with content', function () {
    var content = 'Hello Divider!';
    var divider = (0, _enzyme.mount)(_react["default"].createElement(_Divider["default"], null, content));
    var expected = content;
    var actual = divider.text();
    expect(actual).toBe(expected);
  });
  test('should not capitalize content when casing is "preserve"', function () {
    var content = 'uncapped';
    var divider = (0, _enzyme.mount)(_react["default"].createElement(_Divider["default"], {
      casing: "preserve"
    }, content));
    var expected = content;
    var actual = divider.text();
    expect(actual).toBe(expected);
  });
});