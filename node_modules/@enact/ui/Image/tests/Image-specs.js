"use strict";

var _react = _interopRequireDefault(require("react"));

var _enzyme = require("enzyme");

var _Image = _interopRequireDefault(require("../Image"));

var _ImageModule = _interopRequireDefault(require("../Image.module.css"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

var src = {
  'hd': 'http://lorempixel.com/64/64/city/1/',
  'fhd': 'http://lorempixel.com/128/128/city/1/',
  'uhd': 'http://lorempixel.com/256/256/city/1/'
};
describe('Image Specs', function () {
  test('should only have image class without sizing', function () {
    var image = (0, _enzyme.mount)(_react["default"].createElement(_Image["default"], {
      src: src,
      sizing: "none"
    }));
    var expected = _ImageModule["default"].image;
    var actual = image.find('div').prop('className');
    expect(actual).toBe(expected);
  });
  test('should have class for fill', function () {
    var image = (0, _enzyme.mount)(_react["default"].createElement(_Image["default"], {
      src: src,
      sizing: "fill"
    }));
    var expected = true;
    var actual = image.find('div').hasClass(_ImageModule["default"].fill);
    expect(actual).toBe(expected);
  });
  test('should have class for fit', function () {
    var image = (0, _enzyme.mount)(_react["default"].createElement(_Image["default"], {
      src: src,
      sizing: "fit"
    }));
    var expected = true;
    var actual = image.find('div').hasClass(_ImageModule["default"].fit);
    expect(actual).toBe(expected);
  });
  test('should set role to img by default', function () {
    var image = (0, _enzyme.shallow)(_react["default"].createElement(_Image["default"], {
      src: src,
      sizing: "fit"
    }));
    var expected = 'img';
    var actual = image.prop('role');
    expect(actual).toBe(expected);
  });
});